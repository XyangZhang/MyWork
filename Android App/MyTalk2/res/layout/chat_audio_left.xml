<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingTop="10dip"
    android:paddingBottom="10dip">
    <ImageView 
        android:id="@+id/chatting_pic"
        android:layout_width="@dimen/pic_dimen"
        android:layout_height="@dimen/pic_dimen"
        android:layout_marginLeft="10dip"/>
    
    <LinearLayout 
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:background="@drawable/msg_left_background"
        android:layout_toRightOf="@+id/chatting_pic"
        android:layout_marginTop="10dip"
        android:layout_marginRight="5dip">
        <ImageView 
            android:id="@+id/audio_left_pic"
            android:layout_width="wrap_content"
            android:layout_height="30dip"
            android:src="@drawable/left_audio"/>
    </LinearLayout>

</RelativeLayout>